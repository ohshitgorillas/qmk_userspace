#pragma once

// Userspace configuration options
// This file is processed before the keyboard's config.h

// Add any shared configuration here that applies to all keyboards
// For example:
// #define TAPPING_TERM 200
// #define LEADER_TIMEOUT 300

// Note: Keyboard-specific configs should stay in each keyboard's config.h
